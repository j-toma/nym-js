// this should be present on the view element div
// if on, you it should clear the input box
// if off, if should set the input to the value of element clicked
// the submit button should respect the dropdown if toggled on
// (it should replace if off, add after if on)


